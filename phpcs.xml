<?xml version="1.0"?>
<ruleset name="WordPress Coding Standards for OpenWP">
	<description>Generally-applicable sniffs for the OpenWP plugin</description>

	<!-- Check all PHP files in directory tree by default. -->
	<file>.</file>

	<!-- Exclude directories -->
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!--
	Prevent errors caused by WordPress Coding Standards not supporting PHP 8.0+.
	See https://github.com/WordPress/WordPress-Coding-Standards/issues/2035
	-->
	<ini name="error_reporting" value="E_ALL &#38; ~E_DEPRECATED" />

	<!-- PHP 7.4 and higher. -->
	<config name="testVersion" value="7.4-"/>

	<!-- Arguments - extension, parallel, colors, show progress, Show sniff codes in all reports -->
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="20"/>
	<arg name="colors"/>
	<arg value="sp"/>
	<arg value="s"/>

	<!-- Rules -->
	<rule ref="WordPress-Core" />
	<rule ref="WordPress-Docs" />
	<rule ref="WordPress-Extra" />
	<rule ref="WordPress.Security.EscapeOutput" />
	<rule ref="WordPress-VIP-Go" />

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="openwp"/>
			</property>
		</properties>
	</rule>

	<rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
		<severity>0</severity>
	</rule>

	<rule ref="Internal.NoCodeFound">
		<severity>0</severity>
	</rule>

	<!-- Exclude rules -->
	<rule ref="WordPress-Core">
		<exclude name="WordPress.Files.FileName.InvalidClassFileName" />
		<exclude name="WordPress.PHP.YodaConditions.NotYoda" />
	</rule>
	<rule ref="WordPress-VIP-Go">
		<exclude name="WordPressVIPMinimum.Functions.RestrictedFunctions.attachment_url_to_postid_attachment_url_to_postid" />
		<exclude name="WordPress.NamingConventions.ValidVariableName.UsedPropertyNotSnakeCase" />
	</rule>
</ruleset>
